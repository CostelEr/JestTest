import { F } from "./7";
test("should return correct results", () => {
  expect(F("J5Bomb6CLaC")).toEqual([74, 67]);
  expect(F("vtMM1")).toEqual([118, 49]);
  expect(F("dSofnWP")).toEqual([100, 80]);
});
